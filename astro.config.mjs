// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    // MENGGANTI INI: Ganti dengan URL GitHub Pages Anda (contoh: https://fizcode.github.io)
    site: 'https://fizcode.github.io',
    // Jika repository Anda bukan root (misal: https://fizcode.github.io/my-portfolio), set base ke '/my-portfolio'
    // base: '/intro-to-astro',
});
